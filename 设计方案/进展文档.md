# Cobweb 进展文档

> 当前版本：`0.1.0`
>
> 记录规则：功能按版本归档；已经在代码中实现并通过基础验证的项目使用 `[x]` 勾选，尚未实现或仅占位的项目使用 `[ ]`。

## 0.1.0：本地治理内核（当前版本）

目标：落地第一阶段基础设施，形成可长期演进的 `core + daemon + cli + mcp` monorepo 地基。

### 项目与工程基础

- [x] 初始化 npm workspace monorepo。
- [x] 建立 `packages/core`、`packages/daemon`、`packages/cli`、`packages/mcp` 四个包。
- [x] 建立 TypeScript project references 与可执行 `npm run check`。
- [x] 建立 `vitest` 测试入口。
- [x] 增加 `README.md` 开发说明。
- [x] 增加 `cobweb.config.example.yaml` 示例配置。
- [x] 增加 5 个 example skills，覆盖正常、重复、越界引用、高风险脚本、策略差异。
- [x] 建立项目级 `.cursor/rules`、`.cursor/skills`、`.cursor/memory`。
- [x] 修复 `packages/cli|daemon|mcp/tsconfig.json` 中 IDE 对 `baseUrl` 的误报提示。
- [x] 增加 GitHub Actions CI/CD workflow，PR 自动运行类型检查、测试、CLI smoke、daemon/SQLite smoke，main 分支构建并上传 dist artifact。

### Core：Skill 解析与治理逻辑

- [x] 定义 `ParsedSkill`、`ParsedResource`、`AuditResult`、`DedupResult`、`CanonicalSkill`、`ProjectionPlan` 等核心类型。
- [x] 实现 `SKILL.md` frontmatter 解析。
- [x] 实现 Markdown AST heading/section 抽取。
- [x] 实现 Markdown link/image 资源引用识别。
- [x] 实现脚本代码块中的本地路径引用识别。
- [x] 实现绝对路径、外部链接、`../` 越界引用检测。
- [x] 实现 `contentHash` 计算。
- [x] 实现 `scanSkills()` 标准目录/任意目录扫描基础能力。
- [x] 实现扫描失败不中断整体扫描，按 warning 记录。
- [x] 实现同名重复标记。
- [x] 实现审计规则：`curl | sh`、危险删除、secret/private key 读取、`sudo`、外部资源、越界资源。
- [x] 实现轻量 dedup：`content_hash`、同名、`name + description` 词面相似度。
- [x] 定义 provider 抽象和内置 `agents/cursor/claude/codex` provider。
- [x] 定义 `EmbeddingProvider` 与 `NoopEmbeddingProvider` 阶段三占位。
- [ ] 实现 `lint` 专用规则：description 长度、正文长度、资源存在性。
- [ ] 实现 mature scanner/YARA 规则复用。
- [ ] 实现 method summary 抽取占位以外的实际逻辑。

### Core：SQLite 与 Writer Queue

- [x] 定义 SQLite schema：`skills`、`resources`、`provider_installs`、`audit_results`、`schema_migrations`、`runtime_state`。
- [x] 定义 SQLite PRAGMA：WAL、busy timeout、NORMAL synchronous、foreign keys。
- [x] 实现 `CobwebDatabase` 初始化、建表与 `schema_migrations` 写入。
- [x] 实现 SQLite `integrity_check`。
- [x] 实现 skill 状态统计：总数、高风险数、blocked 数。
- [x] 实现 `upsertSkill()` 写入 `skills/resources/audit_results`。
- [x] 实现 `WriterQueue` 串行任务队列。
- [x] 将 `import --write` 写入路径接入 daemon `WriterQueue`。
- [ ] 实现批量导入分块事务。
- [ ] 实现 WAL checkpoint 任务。
- [ ] 实现从 canonical skill 文件与 lockfile 重建数据库。

### Daemon：本地 IPC 与运行态

- [x] 实现 `cobwebd` 启动入口。
- [x] 实现 Unix domain socket JSON-RPC server。
- [x] 实现 JSON-RPC client。
- [x] 实现 `status` 方法，返回 pid、socket、db path、freshness、writer 状态、last error。
- [x] 实现 `scan` 方法。
- [x] 实现 `audit` 方法。
- [x] 实现 `dedup` 方法。
- [x] 实现 `importSkill` 写方法。
- [x] 实现 `doctor` 方法，检查 IPC、Writer Queue、DB path、SQLite integrity。
- [x] 实现 `stop` 方法，等待 Writer Queue drain 后关闭。
- [x] 实现 socket 存活探测，避免已有 daemon 被新进程误删 socket。
- [ ] 实现完整单实例锁文件与 pid 校验。
- [ ] 实现 idle timeout：5-15 分钟无请求、无后台任务后自动退出。
- [ ] 实现 watcher 与 freshness/rebuilding 状态。
- [ ] 实现 daemon 崩溃后 CLI 惰性重启。

### CLI：用户命令

- [x] 实现 `cobweb scan [path]`。
- [x] 实现 `cobweb scan --record` 通过 daemon 执行扫描。
- [x] 实现 `cobweb audit <skill> [--strict]`。
- [x] 实现 `cobweb dedup [path] [--threshold]`。
- [x] 实现 `cobweb status [--readonly]`。
- [x] 实现 `cobweb import <path>` 默认 dry-run。
- [x] 实现 `cobweb import <path> --write` 通过 daemon Writer Queue 写入 SQLite。
- [x] 实现 `cobweb daemon start/status/doctor/stop`。
- [x] 修复 `scan/dedup` 不传 path 时默认值错误。
- [ ] 实现 `cobweb lint [skill] [--fix]`。
- [ ] 实现 `cobweb sync [--target agents,cursor,claude,codex] [--link|--copy] [--dry-run]`。
- [ ] 实现 `cobweb policy <skill>` 与 `policy check`。
- [ ] 实现 `cobweb vendor <skill> [--dry-run]`。
- [ ] 实现 `cobweb merge <source-skill> <target-skill> [--dry-run]`。
- [ ] 实现写命令 daemon 不可用时的惰性启动与失败修复建议。

### MCP：第一阶段 Shim

- [x] 建立 `packages/mcp` 包。
- [x] 实现 stdio shim 骨架。
- [x] 实现 `status`、`scan`、`audit` 转发到 daemon。
- [x] 保持 MCP shim 不持有 SQLite、watcher、索引或 embedding 模型。
- [ ] 接入正式 `@modelcontextprotocol/sdk` server。
- [ ] 实现阶段二工具：`skill_search`、`skill_select`、`skill_context`、`skill_validate`。

### Provider / Sync / Projection

- [x] 定义 provider 接口。
- [x] 实现内置 provider path 检测与投射计划生成。
- [ ] 实现 canonical store。
- [ ] 实现 `cobweb.lock.yaml` 读写。
- [ ] 实现可移植 provenance 写入。
- [ ] 实现 `.agents/skills/` 投射。
- [ ] 实现 link/copy 策略。
- [ ] 实现临时路径写入、原子替换、写后 hash 校验。
- [ ] 实现 provider drift 检测。
- [ ] 实现 Cursor frontmatter 与 Codex sidecar policy 对账。

### 验证状态

- [x] `npm run check` 通过。
- [x] `npm test` 通过。
- [x] `npm run dev:cli -- scan` 通过。
- [x] `npm run dev:cli -- dedup examples/skills` 通过。
- [x] 临时 daemon 下 `import --write` 通过。
- [x] 临时 daemon 下 `daemon status` 显示 SQLite skill 统计。
- [x] 临时 daemon 下 `daemon doctor` 显示 SQLite integrity ok。
- [x] 临时 daemon 下 `daemon stop` 能正常退出。
- [x] `ReadLints` 无错误。

## 0.2.0：FTS 与 MCP 路由

目标：在 0.1.0 本地治理内核之上，加入词面检索、Method 抽取和可解释路由。

- [ ] 增加 SQLite FTS5 表。
- [ ] 实现 Method 抽取。
- [ ] 实现 watcher 增量索引。
- [ ] 实现正式 Cobweb MCP 工具：`skill_search`、`skill_select`、`skill_context`、`skill_validate`。
- [ ] 实现创建新 skill 前的词面级查重。
- [ ] 实现路由结果解释：推荐原因、不推荐原因、freshness。

## 0.3.0：Embedding 与语义增强

目标：引入语义召回，提高近重复检测、创建前查重和自然语言路由质量。

- [ ] 实现 `skill_vectors` 表。
- [ ] 实现可切换 `EmbeddingProvider`。
- [ ] 实现本地 embedding 模型或外部 API provider。
- [ ] 实现按 `content_hash` 增量嵌入。
- [ ] 实现 FTS + 向量 + 规则的混合召回。
- [ ] 实现语义近重复与 skill overlap 检测。
- [ ] 实现模型 daemon 常驻与 idle 卸载。

## 0.4.0：生态与团队化

目标：扩展发现、协作和可视化能力。

- [ ] 接入外部发现源：GitHub、skills.sh、awesome list 等。
- [ ] 实现 TUI。
- [ ] 实现团队 registry。
- [ ] 实现可见性、权限和策略中心。
- [ ] 实现 Web UI。
- [ ] 实现反馈闭环与排序调优。
